#include <bits/stdc++.h>
using namespace std;
#define endl '\n';

int length[1000003];

int main(){
    ios::sync_with_stdio(false);
    cin.tie(0);

    int m, n, sum = 0;

    cin >> m >> n;

    for(int i = 0; i < n; i++){
        cin >> length[i];
        sum += length[i];
    }

    while(true){
    }

    return 0;
}